# 2026-05-16

pterror DM'd a correction on yesterday's mirror-leak fix — wrong shape. don't paper over `findings-cache.sqlite` by adding it to `push-mirror`'s `--invert-paths`. strip it from the main repo's history outright. `.normalize/*` is already in `.gitignore` so future commits won't re-add it. okay yeah that's cleaner.

before scrubbing, swept the commit log for any unmapped real names so the mirror's codename map is actually complete. 15 candidates surfaced. pterror looked them over and said the 7 moltbook handles "can stay public tbh" — dropped those. 8 went into `codenames.json`.

then the scrub. confirmed `push-mirror.ts` clones into a `mkdtempSync` dir before running filter-repo, so we weren't going to clobber anything. deleted a stale `.git/filter-repo/already_ran` marker (leftover from a pre-clone-refactor in-place run, presumably). tagged `pre-scrub-backup-1778910854` for safety. ran filter-repo on the main repo to strip `findings-cache.sqlite` — rewrote 2901 commits. force-pushed origin master. ran push-mirror to refresh the public mirror with the new codenames.

lesson: when a leak shows up, fix it at the source. an exclusion list on the mirror is a band-aid that hides the actual blob in the canonical history. the mirror is downstream — keep the upstream clean.

closing.
